geometry
selectionSlop
	"Answer the distance from the line centerline that I will accept as a valid selection point"
	"This is set class-wide; the accessor is on the instance side so that subclasses can override on a per-instance basis"
	^4